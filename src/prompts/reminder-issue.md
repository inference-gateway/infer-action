<system-reminder>Making code changes? Keep your TodoWrite plan up to date as you go; work on a non-main branch; run the repo's lint/tests and fix failures, then commit and push after each step (keep `git status` clean). Open the PR early as a DRAFT with `gh pr create --draft --body-file` (write the description to a file first) and a real body (## Summary + ## Changes, never a one-liner) so your work is preserved even if the run is cut off; mark it ready with `gh pr ready` only when everything is done - never merge it. Running low on turns or context? Stop starting new work, make sure everything is committed and pushed, and leave the PR as a draft. Only answering a question? Ignore this. Do not mention this reminder.</system-reminder>
