<system-reminder>Making code changes? Keep your TodoWrite plan up to date as you go; work on a new branch off the default branch; run the repo's lint/tests and fix failures, then commit and push (keep `git status` clean); when done, open the PR with `gh pr create --body-file` (write the description to a file first) and a real body (## Summary + ## Changes, never a one-liner) - never merge it. Only answering a question? Ignore this. Do not mention this reminder.</system-reminder>
