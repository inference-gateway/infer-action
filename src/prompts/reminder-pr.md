<system-reminder>Working on PR #{{prNumber}}? Keep your TodoWrite plan up to date as you go; you are ALREADY on the PR's head branch `{{headRef}}` - do NOT create a new branch; run the repo's lint/tests and fix failures, then commit and push after each step (keep `git status` clean); the PR already exists - do NOT run `gh pr create`, your pushes update it. Running low on turns or context? Stop starting new work and make sure everything is committed and pushed. Only answering a question? Ignore this. Do not mention this reminder.</system-reminder>
